server:
  port: 8081

spring:
  application:
    name: ai-service

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: adminpassword

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.0

app:
  financial-service:
    url: http://financial-service:8084